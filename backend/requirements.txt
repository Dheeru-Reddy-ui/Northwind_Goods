# --- Core API / agent ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-dotenv==1.0.1
httpx==0.28.1
PyYAML==6.0.2
sse-starlette==2.2.1

# --- Optional providers (system runs without these) ---
anthropic==0.42.0            # real LLM; falls back to deterministic engine if key absent
psycopg2-binary==2.9.10      # only needed when DATABASE_URL points at Postgres/Supabase

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
